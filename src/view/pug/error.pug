extends base

block content
  .page-wrap.d-flex.flex-row.align-items-center
    .container
      .row.d-flex.justify-content-center
        .col-md-12.text-center
          span.display-1.d-block= code
          .mb-4.lead= `${msg} 🥺`
          a.btn.btn-light(href='/') Back to Home